<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kelompok1.portalpengaduanuser">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.SplashActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.LoginActivity" />
        <activity android:name=".activity.RegistrasiActivity" />
        <activity android:name=".activity.FasilitasActivity" />
        <activity android:name=".activity.OrganisasiActivity" />
        <activity android:name=".activity.DosenActivity" />
        <activity android:name=".activity.AdministrasiActivity" />
        <activity android:name=".activity.ListDataDosenActivity" />
        <activity android:name=".activity.GantiPasswordActivity" />
        <activity android:name=".activity.TentangKamiActivity" />
        <activity
            android:name=".activity.MainActivity"
            android:theme="@style/AppTheme2.NoActionBar" />
        <activity android:name=".activity.Register"></activity>
    </application>

</manifest>